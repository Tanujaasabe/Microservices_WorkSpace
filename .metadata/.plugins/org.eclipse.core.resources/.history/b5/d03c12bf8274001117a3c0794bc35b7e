server:
  port: 8087

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/username/microservice-config.git
          clone-on-start: true